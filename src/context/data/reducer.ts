import { DataContextAction } from "./action"
import { DataContextState } from "./context"


export const dataContextReducer = (club: DataContextState, action: DataContextAction): DataContextState => {
    switch (action.type) {
        
    }
    return club
}
